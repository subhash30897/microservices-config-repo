spring:
  datasource:
    url: jdbc:mysql://localhost:3306/department_db
    username: root
    password: Root@30081997@
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        
